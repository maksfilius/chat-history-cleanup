{
  "name": "chat-cleanup",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node --test test/*.test.ts",
    "package": "npm run build && cd dist && zip -qr ../chat-cleanup.zip . && cd .. && echo \"chat-cleanup.zip ready\""
  },
  "devDependencies": {
    "@types/chrome": "^0.0.278",
    "@types/node": "^26.4.1",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3"
  }
}
