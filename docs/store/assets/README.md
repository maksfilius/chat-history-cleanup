# Chrome Web Store graphics

The 440×280 promotional tile is generated with `npm run build:store-assets` from the canonical
logo, with all network access blocked.

Store screenshots are deliberately not generated from the landing page. Capture them from the
final unpacked extension on real ChatGPT using a disposable account or staged conversations. Crop
the browser frame and account menu, and verify that no real name, email, title, message text, avatar,
workspace name, or account identifier is visible.
